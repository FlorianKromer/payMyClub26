{% extends 'PMCMainBundle::layout.html.twig' %}

{% block title %}{{ "Accueil"| trans}} {{ parent() }}  {% endblock %}
{% block container %}

<main role="main" class="main">
    <!-- Content goes here -->
    <div class="container">
        <div class="jumbotron">
            <h1>Hello.</h1>
        </div>
    </div>
    <div class="container">
        <!-- Example row of columns -->
        <div class="row">
            <div class="col-md-6">
                <h2>{{ "Bienvenue !"| trans}}</h2>
                <p>Ton texte ici
                </p>
                <a class="btn btn-primary" href="{{ path("pmc_main_index") }}" role="button">{{ "Voir plus &raquo;"| trans |raw}}</a>

            </div>
            <div class="col-md-6">
                <p>et ici!</p>
                <a class="btn btn-primary" href="{{ path("pmc_main_index") }}" role="button">{{ "Voir plus &raquo;"| trans |raw}}</a>
            </div>
        </div>       
    </div>
</main>

{% endblock %}
{% block javascripts %}
{{ parent() }}
<script type="text/javascript">
	

</script>
{% endblock %}